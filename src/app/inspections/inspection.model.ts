export class Inspection {
  id: string;
  name: string;
  date: string;
  valid: string;

  constructor(id: string, name: string, date: string, valid: string) {
    this.id = id;
    this.name = name;
    this.date = date;
    this.valid = valid;
}}
